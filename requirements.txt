# requirements.txt — Ascent web app dependencies
# Install with: pip install -r requirements.txt

fastapi>=0.110.0
uvicorn[standard]>=0.29.0
jinja2>=3.1.3
python-multipart>=0.0.9
httpx>=0.27.0
python-dotenv>=1.0.1
pydantic>=2.6.0
anyio>=4.3.0

# AI Coach
# (httpx already listed above)

# macOS menu-bar icon for .app bundle
rumps>=0.4.0

# Build tool (not needed at runtime, just for building)
# pyinstaller>=6.5.0
